// import type { PageServerLoad } from "../$types";

// export const load: PageServerLoad = async ({ locals }) => {
//   const { data: aboutmephoto, error: err } =
//     await locals.supabase.storage.from("creator").createSignedUrl('/');
//   console.log(aboutmephoto);
// };
